# TACET ブランドサイト 要件定義書

## 1. プロジェクト概要

架空のファッションブランドのブランドサイトを制作する。ラグジュアリー・ミニマルなトーンを軸に、写真・映像を大きく使ったビジュアル訴求と、先進的な印象を与えるレイアウト表現（カラム構成、フルスクリーン、重なり・ずらし）を組み合わせる。加えて、EC（フロントエンドモック）を組み込み、閲覧から購入検討までの一連の体験をサイト内で完結させる。

## 2. ブランドコンセプト

- **トーン**: ラグジュアリー・ミニマル。余白を活かした高級感、モノトーン基調、上質な素材感の表現を重視する。
- **世界観**: 派手な装飾を避け、写真・タイポグラフィ・余白のバランスで格を出す。先進的な印象を出すために、要素の重なりやグリッドのずらしをアクセントとして使うが、全体のトーンはあくまで静かで洗練されたものとする。

### ブランドアイデンティティ（確定）

| 項目 | 内容 |
|---|---|
| ブランド名 | **TACET**（タセット）。ラテン語で「静止せよ／沈黙」を意味する音楽用語に由来。「静けさを纏う」というコンセプトを表す。 |
| ロゴ | テキストロゴ（大文字・広めのレタースペーシング）。画像ロゴは使用しない。 |
| ブランドステートメント | 「静けさを、纏う。」 |

### カラーパレット（確定）

| 用途 | カラーコード |
|---|---|
| 背景（メイン） | `#F5F3EF`（生成り／オフホワイト） |
| サブ背景 | `#EAE6DE`（グレージュ） |
| テキスト・基調 | `#1A1917`（純黒ではない墨色） |
| アクセント | `#8C8275`（トープ。CTA・ホバー等） |
| ボーダー | `#D8D3C9` |

### フォント（確定）

- 見出し（欧文）: Cormorant Garamond ／ 見出し（和文）: Noto Serif JP
- 本文（欧文）: Inter ／ 本文（和文）: Noto Sans JP
- いずれも Google Fonts を使用し、レタースペーシングを広めに取ることで余白との調和を図る。

## 3. ターゲットユーザー

- ファッション感度の高い20〜40代を想定。
- 詳細なペルソナ（年齢・職業・ライフスタイル等の個別設定）までは定義せず、上記の粒度で運用する。
- PC・スマートフォン双方から閲覧する前提。

## 4. サイトの目的・ゴール

1. ブランドの世界観・コンセプトを視覚的に印象づける。
2. 商品を魅力的に見せ、スタイリング例を通じて着用イメージを喚起する。
3. 商品一覧〜詳細〜カートまでの購買体験をモックとして体験できるようにする。

## 5. サイト構成（サイトマップ）

| # | ページ | ファイル | 概要 |
|---|---|---|---|
| 1 | Home | `index.html` | フルスクリーンビジュアル＋ブランドステートメント |
| 2 | Concept | `concept.html` | ブランドストーリー |
| 3 | Collection | `collection.html` | 商品一覧、カテゴリフィルタ |
| 4 | Product Detail | `product.html` | 商品詳細（カラー・サイズ選択→カート追加） |
| 5 | Cart | `cart.html` | カート内容表示。`localStorage` と連携 |
| 6 | Checkout | `checkout.html` | 購入手続きのモック。注文確認画面まで（決済処理なし） |
| 7 | Lookbook | `lookbook.html` | 着用イメージのビジュアルギャラリー |
| 8 | Stores / Contact | `contact.html` | 店舗情報・お問い合わせ |
| 9 | Legal | `legal.html` | 特定商取引法に基づく表記、プライバシーポリシー等（フッターリンク） |

## 6. コンテンツ要件

### 商品ラインナップ（仮）

- カテゴリ: Outerwear / Tops / Bottoms / Accessories の4カテゴリ
- 商品点数: 計14点程度
- 価格帯: ¥18,000〜¥98,000
- 各商品が持つ情報項目: 商品名、カテゴリ、価格、説明文、カラーバリエーション、サイズ展開、画像

### 画像・映像素材

- 当初はグラデーション＋グレインを重ねたプレースホルダーブロック（`.ph` クラス）で仮組みする方針だったが、2026-09-02 に無料ストック素材（Unsplash / Pexels、商用利用可・クレジット表記不要のライセンス）から実写を選定し、`assets/images/` 配下の該当ファイルへ差し替え済み。
- マークアップ構造は変更せず、`<img>` / `background-image` への差し替えのみで対応した。

## 7. デザイン要件

- **レイアウト方針**: フルスクリーンヒーロー、カラム構成、要素の重なり・ずらし（オフセットグリッド）をアクセントとして使用。全体は静的で余白の多い構成を基調とする。
- **ブレークポイント**: 375px（モバイル）／768px（タブレット）／1024px（タブレット横〜小型PC）／1440px（デスクトップ）
- **インタラクション**: スクロールに応じたフェードイン程度の控えめな演出を基本とし、過度なアニメーションは行わない（トーンを損なわないことを優先）。

## 8. 技術要件

- **使用技術**: HTML / CSS / JavaScript（素のフロントエンド実装。フレームワーク・ビルドツールは使用しない）。
- **ディレクトリ構成**:
  ```
  /
  ├─ index.html, concept.html, collection.html, product.html,
  │  cart.html, checkout.html, lookbook.html, contact.html, legal.html
  ├─ css/style.css
  ├─ js/main.js（共通処理・ヘッダーのカート件数表示）
  ├─ js/cart.js（カートのCRUD・localStorage連携）※実装予定
  ├─ js/products.js（商品モックデータ）※実装予定
  └─ assets/images/
  ```
- **バックエンド**: なし。EC機能はフロントエンドのみで完結するモック実装とする。
  - カート情報は `localStorage`（キー: `tacet_cart`）に保存し、ページ遷移・リロード後も保持する。
  - 実際の決済・在庫連携・会員機能は対象外（将来拡張の余地として設計はしておく）。
- **対応ブラウザ**: 最新版の Chrome / Safari / Edge / Firefox。
- **対応デバイス**: PC・スマートフォン両対応のレスポンシブデザイン。タブレットは中間ブレークポイントで対応。
- **対応言語**: 日本語のみ。
- **ローカル確認用サーバー**: ビルド不要の静的サイトのため、リポジトリ直下で下記コマンドを実行し、ブラウザで開いて確認する。
  ```bash
  python3 -m http.server 8080
  ```
  起動後、ブラウザで `http://localhost:8080/` を開く（WSL2環境ではWindows側のブラウザから `localhost` で直接アクセス可能）。
- **リポジトリ**: https://github.com/code123-arch/fassion （2026-09-03 に push 開始）。push には Personal Access Token（repo権限、2026-09-03発行・約30日で失効）を使用。トークン自体はどこにも記載しないこと（漏洩防止のため、失効後は再発行して各自のローカル環境でのみ保持する）。

## 9. 非機能要件（最低限）

- 画像は遅延読み込み（`loading="lazy"`）を基本とする。
- インタラクティブ要素（ナビゲーション、カートボタン等）には最低限の `aria-label` 等を付与し、キーボード操作を妨げない構造にする。
- パフォーマンス目標値やWCAG準拠レベルなど、本番運用を前提とした詳細な基準は本プロジェクトでは定めない（デモ・ポートフォリオ用途のため）。

## 10. 対象外事項（Out of Scope）

- 実際の決済処理・決済代行連携
- 在庫管理・会員登録／ログイン機能
- 実写真・実映像素材の制作
- 多言語対応（日本語以外）
- 詳細なSEO対策・アクセシビリティ監査

## 11. 現在の進捗

- ブランドアイデンティティ（名称・カラー・フォント）決定済み
- デザイントークン・共通スタイル（`css/style.css`）実装済み
- Home ページ（`index.html`）実装済み（ヘッダー、ヒーロー、オフセットグリッド、New Arrivals、Concept teaser、フッター）
- 共通スクリプト（`js/main.js`：ヘッダーのカート件数表示）実装済み
- 未着手: Concept / Collection / Product Detail / Cart / Checkout / Lookbook / Contact / Legal の各ページ、カートロジック（`js/cart.js`）、商品モックデータ（`js/products.js`）
